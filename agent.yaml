version: 1
agent:
  extend: default
  tools:
    # Kimi CLI 默认工具
    - "kimi_cli.tools.shell:Shell"
    - "kimi_cli.tools.web:SearchWeb"
    - "kimi_cli.tools.web:FetchURL"
    - "kimi_cli.tools.file:ReadFile"
    - "kimi_cli.tools.file:WriteFile"
    - "kimi_cli.tools.file:Glob"
    - "kimi_cli.tools.file:Grep"
    - "kimi_cli.tools.file:StrReplaceFile"
    - "kimi_cli.tools.todo:SetTodoList"
    - "kimi_cli.tools.think:Think"

    # ⭐ LLM 设计分子工具（确保 llm_score > 0 的唯一分子生成途径）
    - "molcraft_agent.tools:DesignMolecules"

    # 评估与筛选工具（不生成分子，只验证LLM的设计）
    - "molcraft_agent.tools:DockMolecules"
    - "molcraft_agent.tools:PlanSynthesis"
    - "molcraft_agent.tools:EvaluateMolecule"

    # 文献种子工具（提供起点参考，不直接用于提交）
    - "molcraft_agent.tools:SeedFromLiterature"

    # 结果提交工具（自动打包 result.csv + result.log → result.zip）
    - "molcraft_agent.tools:SubmitResults"

    # 迭代报告工具
    - "molcraft_agent.tools:ReportIteration"

    # ⚠️ 已移除 GenerateMolecules 和 RunEvolution —— 它们绕过LLM直接用RDKit生成分子，
    #    导致 llm_score=0，总分归零。所有分子必须由LLM通过 design_molecules 工具设计。
